# Git worktrees for concurrent work live under the repo so the family CLAUDE.md
# still loads inside them (see the umbrella's "Working across repos"). They are
# never tracked — each is a checkout of this same repo.
.worktrees/
